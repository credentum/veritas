# Veritas Environment Configuration
# Copy this to .env and fill in your values

# AO Process ID for receipt storage
# Get this from running: npm run spawn
VERITAS_PROCESS_ID=

# (Optional) Custom wallet path
# Default: ./wallets/wallet.json or ~/.aos.json
# WALLET_PATH=/path/to/wallet.json

# (Optional) Server port
# Default: 3000
# PORT=3000
